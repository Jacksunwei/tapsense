{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/tapsense-vscode"],
      "outFiles": ["${workspaceFolder}/tapsense-vscode/out/**/*.js"],
      "preLaunchTask": "npm: compile - tapsense-vscode"
    }
  ]
}
